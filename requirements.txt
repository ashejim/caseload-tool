playwright>=1.40
python-dotenv>=1.0
pyyaml>=6.0
pynput>=1.7
customtkinter>=5.2
Pillow>=10.0
pywin32>=305 ; sys_platform == "win32"
# Windows/standalone Python ships no system tz database; zoneinfo needs this
# for the timezone-aware texting scheduler (and student-local-time display).
tzdata>=2024.1
